# ─────────────────────────────────────────────────────────
# Madina Lab — Environment Variables
# ─────────────────────────────────────────────────────────
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# These are used by Vite at BUILD TIME (not runtime).
# In production, values come from GCP Secret Manager via Cloud Build.
# ─────────────────────────────────────────────────────────

# ── Firebase Configuration (Required) ────────────────────
# Get these from: Firebase Console → Project Settings → General → Your apps
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=

# ── Google Maps (Optional) ──────────────────────────────
# Get from: Google Cloud Console → APIs & Services → Credentials
VITE_GOOGLE_MAPS_API_KEY=

# ── Namespace Isolation (Optional) ──────────────────────
# Set your name to isolate your data in the shared Firestore.
# Example: VITE_NAMESPACE=alice → collections become alice_users, alice_events, etc.
# Leave empty to use default collection names.
VITE_NAMESPACE=

# ── App Settings (Optional) ─────────────────────────────
# Environment: development | production
VITE_APP_ENV=development
# App URL (used for links, redirects)
VITE_APP_URL=http://localhost:8080
